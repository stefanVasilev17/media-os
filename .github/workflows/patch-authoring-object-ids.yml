name: Patch authoring object ids

on:
  push:
    paths:
      - '.github/workflows/patch-authoring-object-ids.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Apply patch
        run: python scripts/patch_authoring_object_ids.py
      - name: Compile backend
        working-directory: backend
        run: mvn -q -DskipTests compile
      - name: Validate direct helper
        run: node --check worker/spline-direct-overlay.js
      - name: Validate worker syntax
        shell: pwsh
        run: |
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'worker/spline-worker.ps1'), [ref]$tokens, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }
      - name: Remove temporary patch files
        run: rm scripts/patch_authoring_object_ids.py .github/workflows/patch-authoring-object-ids.yml
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add backend/src/main/java/com/architecturalthinking/mediaos/spline/SplineCapabilityCatalogService.java backend/src/main/java/com/architecturalthinking/mediaos/spline/SplineSceneSlotRegistryService.java backend/src/main/java/com/architecturalthinking/mediaos/spline/SplineAuthoringOverlayRegistryService.java worker/spline-worker.ps1 worker/spline-direct-overlay.js scripts/patch_authoring_object_ids.py .github/workflows/patch-authoring-object-ids.yml
          git commit -m 'Preserve Spline authoring object ids in scene knowledge'
          git push origin main
