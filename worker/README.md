# Production Worker

The Windows worker will receive approved `ProductionJob` contracts from Media OS, execute them locally next to Spline Desktop / DaVinci Resolve, upload preview artifacts, and report structured results. v0.1 keeps this as a contract boundary; real Spline MCP integration is the next vertical slice.
